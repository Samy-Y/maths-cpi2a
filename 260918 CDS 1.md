# Retranscription du contenu du cours de soutien

<!-- "le cours peut être un problème" - faouzi -->

**Définition d'une algèbre :**
$(A,+,\times,\cdot)$ est une algèbre si :
1. $(A,+,\times)$ est un anneau
2. $(A,\cdot)$ est un espace vectoriel
3. $\forall x,y \in A,\forall \lambda \in \mathbb{K}, \lambda(x \times y) = (\lambda x) \times y = x \times (\lambda y)$

**Remarque :**
- Si $A$ est une algèbre associative et $a = \sum_{i=1}^n \alpha_i x_i \in A$ et $b= \sum_{j=1}^m \beta_j y_j \in A$, alors $a \times b = \sum_{i=1}^n \sum_{j=1}^m \alpha_i \beta_j (x_i \times y_j)$
- La dimension d'une algèbre est égale à la dimension de l'espace vectoriel $(A,+,\cdot)$.

**Définition d'une sous-algèbre :**
Soit $(A,+,\times,\cdot)$ une algèbre. Soit $B \subset A$. On dit que $B$ est une sous-algèbre de $A$ si :
1. Stabilité par multiplication : $\forall x,y \in B, x \times y \in B$
2. Stabilité par combinaison linéaire : $\forall x,y \in B, \forall \lambda \in \mathbb{K}, \lambda x + y \in B$
3. $1_A \in B$

**Exemple :**
Soit $E$ un $\mathbb K$-espace vectoriel.
Soit $u \in \mathcal L(E)$ et $\varphi(u) = \{v \in \mathcal L(E) | uv = vu\}$. Montrons que $\varphi(u)$ est une sous-algèbre de $\mathcal L(E)$.

* L'application $Id_E$ est dans $\varphi(u)$ car $u I_d = I_d u$.
* Soient $v_1, v_2 \in \varphi(u)$ et $\lambda \in \mathbb{K}$. Alors :
$$u(\lambda v_1 + v_2) = \lambda uv_1 + uv_2 = \lambda v_1 u + v_2 u = (\lambda v_1 + v_2)u$$
* Donc $\lambda v_1 + v_2 \in \varphi(u)$.
* Soient $v_1, v_2 \in \varphi(u)$. Alors :
$$u(v_1 v_2) = (uv_1)v_2 = (v_1 u)v_2 = v_1(uv_2) = v_1(v_2 u) = (v_1 v_2)u$$
* Donc $v_1 v_2 \in \varphi(u)$.

On a donc montré que $\varphi(u)$ est une sous-algèbre de $\mathcal L(E)$.

**Définition d'un morphisme d'algèbres :**
Soient $(A,+,\times,\cdot)$ et $(B,+,\otimes,\cdot)$ deux algèbres. Une application $\phi: A \to B$ est un morphisme d'algèbres $\iff$
$$\begin{cases}
\varphi(x \times y) = \varphi(x) \otimes \varphi(y) \\
\varphi(\lambda x + y) = \lambda \varphi(x) + \varphi(y) \\
\varphi(1_A) = 1_B
\end{cases}$$

**Exemple :**
$$\varphi : \mathbb{K}[X] \to A$$
$$P \mapsto P(a)$$
$$\text{Avec } P(a) = \sum_{i=0}^n \alpha_i a^i$$

> $\varphi$ est appelé le morphisme d'évaluation en $a$.

Montrons que $\varphi$ est un morphisme d'algèbres.

1. Soient $P, Q \in \mathbb{K}[X]$. Alors :
$$\varphi(PQ) = (PQ)(a) = P(a) \times Q(a) = \varphi(P) \times \varphi(Q)$$
2. Soient $P, Q \in \mathbb{K}[X]$ et $\lambda \in \mathbb{K}$. Alors :
$$\begin{align*}
\varphi(\lambda P + Q) &= (\lambda P + Q)(a) \\
&= \lambda \varphi(P) + \varphi(Q)
\end{align*}$$
3. On a $\varphi(1_{\mathbb{K}[X]}) = 1_A$. *(Préférable de toujours le vérifier en premier).*

> Il faut se rappeller que $PQ(f) = P(f) \times Q(f) = Q(f) \times P(f)$ pour un endomorphisme $f$ parce que les endomorphismes commutent avec eux-mêmes et l'identité, ce qui est le contenu de ces polynômes.

>[!WARNING] Rappel de cours
> L'image d'un morphisme d'algèbres est une sous-algèbre de l'algèbre d'arrivée. En effet, si $\varphi : A \to B$ est un morphisme d'algèbres, alors $\mathrm{Im}(\varphi) = \{\varphi(x) | x \in A\}$ est une sous-algèbre de $B$.
>
> *C'est comme ça que l'on peut montrer que $\mathbb{K}[f]$ est une sous-algèbre de $\mathcal L(E)$ pour un endomorphisme $f$, ou que pour n'importe quel élément $a$ d'une algèbre $A$, $\mathbb{K}[a]$ est une sous-algèbre de $A$.*
>
> *On peut aussi montrer que $\mathbb{K}[a]$ est une algèbre en vérifiant les propriétés de base...*

>[!WARNING] Autre rappel utile
> Deux endomorphismes sont égaux s'ils sont égaux sur une base de l'espace vectoriel. C'est-à-dire que si $f, g \in \mathcal L(E)$ et $B$ est une base de $E$, alors :
> $$f = g \iff \forall e_i \in B, f(e_i) = g(e_i)$$

Montrons que $\mathbb{K}[a]$ est la plus petite sous-algèbre de $A$ contenant $a$. Soit $B$ une sous-algèbre de $A$ contenant $a$.

Comme $B$ est une sous-algèbre, elle est stable par combinaison linéaire et par multiplication.
$$\forall k \in \N^*,a^k \in B$$
$$\implies \forall k \in \N^*, \sum \alpha_i a^k \in B$$

Donc pour tout polynôme $P \in \mathbb{K}[X]$, on a $P(a) \in B$. Ainsi $\mathbb{K}[a] \subset B$. Donc $\mathbb{K}[a]$ est la plus petite sous-algèbre de $A$ contenant $a$.

Soit $E$ un $\mathbb K$-espace vectoriel de dimension $n$. Soit $B$ une base de $E$. On définit l'application :

$$\varphi : \mathcal L(E) \to \mathcal M_n(\mathbb{K})$$
$$f \mapsto \mathrm{Mat}_B(f)$$

Montrons que $\varphi$ est un morphisme d'algèbres.

1. L'identité est préservée : $\varphi(Id_E) = I_n$.
2. Soient $f, g \in \mathcal L(E)$. Alors :
$$\varphi(f \circ g) = \mathrm{Mat}_B(f \circ g) = \mathrm{Mat}_B(f) \times \mathrm{Mat}_B(g) = \varphi(f) \times \varphi(g)$$
1. Soient $f, g \in \mathcal L(E)$ et $\lambda \in \mathbb{K}$. Alors :
$$\begin{align*}
\varphi(\lambda f + g) &= \mathrm{Mat}_B(\lambda f + g) \\
&= \lambda \mathrm{Mat}_B(f) + \mathrm{Mat}_B(g) \\
&= \lambda \varphi(f) + \varphi(g)
\end{align*}$$

> On peut écrire sans justifier $\mathrm{Mat}_B(\lambda f + g) = \lambda \mathrm{Mat}_B(f) + \mathrm{Mat}_B(g)$, idem pour $\mathrm{Mat_B}(f \circ g) = \mathrm{Mat}_B(f) \times \mathrm{Mat}_B(g)$.

**Exercice 1.3.1 (p5; fiche de soutien) :**
$u \in \mathcal L(E)$, $E$ un $\mathbb K$-espace vectoriel de dimension finie. On suppose que $\exists x_0 \in E$ tel que $(x_0, u(x_0), \ldots, u^{n-1}(x_0))$ est une base de $E$.

***Montrer que le commutant est une sous-algèbre de $\mathcal L(E)$ (déjà vu dans l'exemple précédent).***

***Montrer que $\mathbb{K}[u] = \{P(u) | P \in \mathbb{K}[X]\}$ est inclus dans le commutant de $u$.***

Soit $P \in \mathbb{K}[X]$ tel que $P(X) = \sum_{i=0}^n \alpha_i X^i$. Alors :
$$\begin{align*}
P(u) \circ u &= (\sum_{i=0}^n \alpha_i u^i) \circ u \\
&= \sum_{i=0}^n \alpha_i u^{i+1} \\
&= u \circ (\sum_{i=0}^n \alpha_i u^i)\\
&= u \circ P(u)
\end{align*}$$

> Alternativement, on peut aussi dire que, comme $\mathbb{K}[u]$ est le plus petit sous-algèbre de $\mathcal L(E)$ contenant $u$, et comme le commutant de $u$ est une sous-algèbre contenant $u$, on a $\mathbb{K}[u] \subset \mathcal C(u)$.

***Soient deux endomorphismes $v$ et $w$ du commutant $\mathcal C(u)$. Montrer que si $v(x_0) = w(x_0)$, alors $v = w$.***
$$\begin{align*}
v(u(x_0)) &= u(v(x_0)) \\
&= u(w(x_0)) \\
&= w(u(x_0))
\end{align*}$$

Avant de procéder par récurrence, on montre que $v$ et $u^k$ commutent pour tout $k \in \{0, \ldots, n-1\}$ (par récurrence aussi). *Le jour de l'examen, on peut utiliser cette propriété pour simplifier les calculs.*

Puis on procède par récurrence pour montrer que $v(u^k(x_0)) = w(u^k(x_0))$ pour tout $k \in \{0, \ldots, n-1\}$.

La relation est vérifiée pour le premier élément. Pour généraliser, on prend $k \in \{0, \ldots, n-1\}$ et on suppose que $v(u^k(x_0)) = w(u^k(x_0))$. Alors :
$$\begin{align*}
v(u^{k+1}(x_0)) &= u(v(u^k(x_0))) \\
&= u(w(u^k(x_0))) \\
&= w(u^{k+1}(x_0))
\end{align*}$$

On a donc démontré par récurrence que $v(u^k(x_0)) = w(u^k(x_0))$ pour tout $k \in \{0, \ldots, n-1\}$. Comme $(x_0, u(x_0), \ldots, u^{n-1}(x_0))$ est une base de $E$, on en déduit que $v = w$.

> On peut aussi calculer $v(x)$ et aboutir via chemin similaire au même résultat.

***Soit $v \in \mathcal C(u)$. Justifier l'existence de $(a_0, \ldots, a_{n-1}) \in \mathbb{K}^n$ tel que $v(x_0) = \sum_{i=0}^{n-1} a_i u^i(x_0)$.***

On sait que $(x_0, u(x_0), \ldots, u^{n-1}(x_0))$ est une base de $E$. Donc pour tout $v \in \mathcal C(u)$, on peut écrire :
$$v(x_0) = \sum_{i=0}^{n-1} a_i u^i(x_0)$$

***Montrer que $v = \sum_{i=0}^{n-1} a_i u^i$.***

On a :
$$\begin{align*}
v(x_0) &= \sum_{i=0}^{n-1} a_i u^i(x_0) \\
&= \left(\sum_{i=0}^{n-1} a_i u^i\right)(x_0)
\end{align*}$$

Donc $v = \sum_{i=0}^{n-1} a_i u^i$.

***Décrire $\mathcal C(u)$***

On a montré que $\mathbb{K}[u] \subset \mathcal C(u) \subset \mathcal L(E)$, vérifié quelque soit $u \in \mathcal L(E)$.

On a aussi montré que $\mathcal C(u) \in \mathbb{K}[u]$ via l'existence de $(a_0, \ldots, a_{n-1}) \in \mathbb{K}^n$ tel que $v = \sum_{i=0}^{n-1} a_i u^i$. Donc $\mathcal C(u) = \mathbb{K}[u]$.

***Déterminer la dimension de $\mathcal C(u)$.***

On sait que $\mathrm{Vect}(Id_E, u, \ldots, u^{n-1}) = \mathbb{K}[u]$. On ne sait pas si cette famille est libre. Soient $(\alpha_0, \ldots, \alpha_{n-1}) \in \mathbb{K}^n$ tel que :
$$\sum_{i=0}^{n-1} \alpha_i u^i = 0_{\mathcal L(E)}$$

Au point $x_0$ en particulier, on a :
$$\sum_{i=0}^{n-1} \alpha_i u^i(x_0) = 0_E$$

Cette famille est libre car $(x_0, u(x_0), \ldots, u^{n-1}(x_0))$ est une base de $E$. Donc $\alpha_i = 0$ pour tout $i \in \{0, \ldots, n-1\}$. Donc la famille $(Id_E, u, \ldots, u^{n-1})$ est libre. Donc $\dim(\mathcal C(u)) = n$.

---

**Application :**
$$J = \left(\begin{matrix} 0 & 0 & 0 & \ldots & 0 & a_0 \\ 1 & 0 & 0 & \ldots & 0 & a_1 \\ \vdots & \vdots & \ddots & \vdots & \vdots & \vdots \\ 0 & 0 & 0 & \ldots & 0 & a_{n-2} \\ 0 & 0 & 0 & \ldots & 1 & a_{n-1} \end{matrix}\right)$$
***Décrire $\mathcal C(J)$ (à faire chez soi).***

---

>[!NOTE] Pour montrer qu'un ensemble est un idéal...
>On montre que c'est un sous-groupe
>On montre qu'il est absorbant par multiplication à gauche et à droite par les éléments de l'anneau.

$I_a = \ker(\phi_a)=\{P \in \mathbb{K}[X] \mid P(a) = 0\}$ est un idéal mais n'est pas une sous-algèbre car $1_A \notin I_a$.

$I_a$ est un **idéal principal** parce que $\mathbb{K}[X]$ est un anneau principal.

On rappelle qu'un idéal principal est un idéal qui est engendré par un seul élément. C'est-à-dire que $\exists P \in \mathbb{K}[X]$ tel que $I_a = \langle P\rangle = \{PQ | Q \in \mathbb{K}[X]\}$.

On choisit un polynôme $P$ **unitaire de degré minimal** dans $I_a$. Alors $I_a = \langle P\rangle$. C'est un *choix* ! Ce choix de polynôme est, par contre, unique.

On suppose que $I_a = \langle \Pi_a\rangle = \langle \Pi'_a\rangle$.

On a $\Pi_a \in I_a = \langle \Pi'_a\rangle$. Donc $\exists Q \in \mathbb{K}[X]$ tel que $\Pi_a = Q \Pi'_a$. Donc $\Pi'_a \mid \Pi_a$. De même, $\Pi'_a \in I_a = \langle \Pi_a\rangle$. Donc $\exists Q' \in \mathbb{K}[X]$ tel que $\Pi'_a = Q' \Pi_a$. Donc $\Pi_a \mid \Pi'_a$. Donc $\Pi_a$ et $\Pi'_a$ sont associés. Comme ils sont tous les deux unitaires, on a $\Pi_a = \Pi'_a$.

**Exemple 1.3.2 (p6; fiche de soutien)**
1. Un endomorphisme $u \in \mathcal L(E)$ est nilpotent d'indice $q \ge 1$ si et seulement si $\pi_u(X) = X^q$.

Dans le sens direct, si $u$ est nilpotent d'indice $q$, alors $u^q = 0$. Donc $\pi_u(X) \mid X^q$. Donc $\pi_u(X) = X^p$ avec $p \le q$. Si $p < q$, alors $u^p = 0$, ce qui contredit le fait que l'indice de nilpotence est $q$. Donc $p = q$.

Dans le sens indirect, si $\pi_u(X) = X^q$, alors $u^q = 0$. Donc $u$ est nilpotent d'indice $q$.

**Exercice bonus :**
Soit $A$ une algèbre de dimension finie. Montrer que $A$ admet un polynôme minimal. Supposons par l'absurde que $A$ n'admet pas un polynôme minimal.

On prend la famille $(1_A,a,a^2,\ldots,a^{n})$. Comme $A$ est de dimension finie $n$, cette famille est liée. Donc $\exists n \in \N^*, \exists (\alpha_0, \ldots, \alpha_n) \in \mathbb{K}^{n+1}$ tel que :
$$\alpha_0 1_A + \alpha_1 a + \ldots + \alpha_n a^n = 0$$

Donc $a$ admet un polynôme annulateur. On choisit un polynôme annulateur de degré minimal. C'est le polynôme minimal de $a$. Donc $A$ admet un polynôme minimal. Contradiction.

**Exercice 1.3.2 (p6; fiche de soutien)**

Soit $x \in E$ et $u \in \mathcal L(E)$ admettant un polynôme minimal $\pi_u(X)$.

Soit $\mathcal N_x = \{P \in \mathbb{K}[X] | P(u)(x) = 0\}$. Montrer que $\mathcal N_x$ est un idéal de $\mathbb{K}[X]$ engendré par un diviseur de $\pi_u(X)$ noté $\Pi_x$.

> On ne peut pas utiliser de relations avec les noyaux : $E$ n'est pas une algèbre.

> On applique jamais un polynôme à un vecteur, mais à un endomorphisme.

- Montrons que $(\mathcal N_x,+)$ est un sous-groupe de $(\mathbb{K}[X],+)$. $(\mathcal N_x,+)$ est non-vide (le polynôme nul), inclus dans $(\mathbb{K}[X],+)$. Soient $P, Q \in \mathcal N_x$. Alors :
$$\begin{align*}
(P-Q)(u)(x) &= P(u)(x) - Q(u)(x) \\
&= 0 - 0 \\
&= 0
\end{align*}$$

Soit $P \in \mathbb K[X]$. Montrons que $P\mathcal N_x \subset \mathcal N_x$. Soit $N \in \mathcal N_x$. Alors :
$$\begin{align*}
(PN)(u)(x) &= P(u) \circ N(u)(x) \\
&= P(u)(N(u)(x)) \\
&= P(u)(0_E) \\
&= 0_E
\end{align*}$$

Un idéal de $\mathbb{K}[X]$ est forcément principal donc engendré par un polynôme unitaire, car $\mathbb{K}[X]$ est un anneau principal. Donc $\mathcal N_x$ est engendré par un polynôme unitaire $\Pi_x$.

Montrons que $\Pi_x \mid \pi_u$. On sait que $\pi_u(u) = 0_{\mathcal L(E)}$. Donc $\pi_u(u)(x) = 0_E$. Donc $\pi_u \in \mathcal N_x$. Donc $\Pi_x \mid \pi_u$.

***Soient $x,y \in E$, et $\Pi_x \wedge \Pi_y = 1$. Déterminer $\Pi_{x+y}$.***

